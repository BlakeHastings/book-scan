/**
 * One book's own page: what it is, what you can do about it, where it sits.
 *
 * Not the review screen, which is the form a record is corrected on. The two are
 * one journey rather than two doors to one room: the pencil in this page's
 * corner opens that form, and coming back out of it lands here.
 */

import { BookPane } from '../components/BookPane'

export function BookScreen() {
  return <BookPane />
}
