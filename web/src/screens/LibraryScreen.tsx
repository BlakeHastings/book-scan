/** `ShelfView` is the shelves as a job of work; this is the library as somebody browses it. */

import { LibraryPane } from '../components/LibraryPane'

export function LibraryScreen() {
  return <LibraryPane />
}
