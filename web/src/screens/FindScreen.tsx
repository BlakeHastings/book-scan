/** This screen wears the library tab rather than its own: you have not gone anywhere, this is the one action in the library's top right. */

import { FindPane } from '../components/FindPane'

export function FindScreen() {
  return <FindPane />
}
